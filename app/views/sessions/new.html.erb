<section class="form-wrapper wrapper">
  <h1>Welcome to Seat Yourself</h1>

  <%= form_for :session, url: sessions_path, html: {class: 'form'} do |f| %>
    <div class="field">
      <%= f.label :email %>
      <%= f.email_field :email, placeholder: 'Enter Email' %>
    </div>

    <div class="field">
      <%= f.label :password %>
      <%= f.password_field :password, placeholder: 'Enter Password' %>
    </div>

    <%= f.submit 'Log In', class: 'btn-default' %>
  <% end %>

  <p class="call-to-action">
    New to Seat Yourself? <%= link_to 'Sign Up!', new_user_path %>
  </p>
</section>